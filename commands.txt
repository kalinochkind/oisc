Память:
0 -- instruction pointer

0. HALT
1. CCOPY val src dst  # copy *src to *dst if val >= 0, -1 is i/o port, jump is copy to 0
2. SUB src dst # dst -= src



MCP $a $b $c =>
SUB $a $c $c
SUB $a $t $t
SUB $a $b $t
SUB $a $t $c


MCP $a $b $zero =>
SUB 0 $t $t
SUB 0 $-cp $t
SUB 0 $b $t
t = cp - b
SUB $a $t $zero
@cp



.int a 10 => $a - само a, &a - адрес a


@label => @label - адрес (для выражений), @ - текущий адрес
@-label => хранится адрес с минусом
